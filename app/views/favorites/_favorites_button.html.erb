<% if book.favorites_by?(current_user) %>
  <%= link_to book.favorites.count, book_favorites_path(book.id), class: "text-danger fas fa-heart", method: :delete %>
<% else %>
  <%= link_to book.favorites.count, book_favorites_path(book.id), class: "fas fa-heart", method: :post %>
<% end %>